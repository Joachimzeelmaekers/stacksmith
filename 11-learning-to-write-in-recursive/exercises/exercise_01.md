# Exercise 1: Character Count in Nested Array

Count the total number of characters in a nested array of strings.

## Key Concepts

- Handle nested arrays recursively
- Base case: string → return its length
- Recursive case: array → sum of all elements

## Run the Solution

- **TypeScript:** `npx tsx src/typescript/chapter11/exercise01.ts`
- **Go:** `go run src/go/chapter11/exercise01/main.go`
