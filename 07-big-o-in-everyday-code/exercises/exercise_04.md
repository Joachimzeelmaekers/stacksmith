# Exercise 4: Missing Number

Given an array containing n-1 numbers from 1 to n, find the missing number.

**Approaches:**
- Sum method: O(N) time, O(1) space - may overflow for large numbers
- XOR method: O(N) time, O(1) space - no overflow ✓
- Set method: O(N) time, O(N) space - simple but uses more memory

## Run the Solution

- **TypeScript:** `npx tsx src/typescript/chapter07/exercise04.ts`
- **Go:** `go run src/go/chapter07/exercise04/main.go`
